name: homepage
version: 1.0.0
description: Tableau de bord MediaStack
container: homepage
category: dashboard

ports:
  - 3000

health:
  protocol: http
  port: 3000
  path: /

dependencies:
  - docker
  - caddy

storage:
  - /opt/media/homepage
